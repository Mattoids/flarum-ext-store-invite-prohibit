import app from 'flarum/common/app';

app.initializers.add('mattoid/flarum-ext-store-invite-prohibit', () => {
  console.log('[mattoid/flarum-ext-store-invite-prohibit] Hello, forum and admin!');
});
